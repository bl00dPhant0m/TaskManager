spring:
  application:
    name: TaskManger

  datasource:
    username:
    password:
    url: jdbc:h2:file:./tasks_bd_test

  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create

  h2:
    console:
      enabled: true


